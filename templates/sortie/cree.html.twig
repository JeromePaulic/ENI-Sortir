{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="text-center">Crée une sortie</h2>
<form>
    <div class="grix xs1 md2">
    <div>
         {{ form_start(sortieForm) }}

         {{ form_row(sortieForm.nom) }}
         {{ form_row(sortieForm.dateHeureDebut) }}
         {{ form_row(sortieForm.nbParticipantMax) }}
         {{ form_row(sortieForm.duree) }}
        {{ form_row(sortieForm.dateLimiteInscription) }}
        {{ form_row(sortieForm.infoSortie) }}
    </div>
    <div>
    {{ form_row(sortieForm.campusOrganisateur) }}
    {{ form_row(sortieForm.lieu) }}
    {{ form_row(sortieForm.etat) }}
    </div>
    </div>
    <button class="btn blue rounded-1">Enregister</button>
    {{ form_end(sortieForm) }}

</form>
{% endblock %}

{% block title %}{{ parent() }}{% endblock %}

