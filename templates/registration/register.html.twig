{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
    <center>
    <h1>S'inscrire</h1>

    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}

        <button type="submit" class="btn blue rounded-1">S'inscrire</button>
    {{ form_end(registrationForm) }}
    </center>
{% endblock %}
